import { PackageColorPipe } from './package-color.pipe';

describe('PackageColorPipe', () => {
	it('create an instance', () => {
		const pipe = new PackageColorPipe();
		expect(pipe).toBeTruthy();
	});
});
